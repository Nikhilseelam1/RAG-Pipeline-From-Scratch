from setuptools import setup, find_packages

setup(
    name="RAG-PIPELINE-FROM-SCRATCH",
    version="0.0.1",
    author="Nikhilseelam1",
    description="RAG Pipeline built from scratch",
    packages=find_packages(),
    install_requires=[],
    python_requires=">=3.9",
)
